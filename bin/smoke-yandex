#!/usr/bin/env bash
set -euo pipefail

cd "$(dirname "$0")/.."

if [ ! -x .venv/bin/python ]; then
  echo "Missing .venv. Run ./bin/onboard first."
  exit 1
fi

.venv/bin/python -m route74.sources.yandex.smoke

echo "OK | yandex smoke passed"
